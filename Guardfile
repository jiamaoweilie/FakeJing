# A sample Guardfile
# More info at https://github.com/guard/guard#readme

  guard 'livereload' do
  watch('index.html')
  watch(%r{css/.+\.(css)})
  watch(%r{sass/.+\.(scss)})
  watch(%r{js/.+\.(js)})
  end

 guard :compass